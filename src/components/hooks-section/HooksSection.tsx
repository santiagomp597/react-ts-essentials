import React from 'react';
import './HooksSection.css';

const HooksSection: React.FC = () => {
  return (
    <section className="hooks-section">
      <h1>Hooks Section (useState, useEffect, useContext)</h1>
    </section>
  );
};

export default HooksSection;